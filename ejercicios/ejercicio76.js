// Nivel: 10
// Consigna: Tipo If condicional simple

function tipoIf(cond, a, b) {
    return cond ? a : b;
}
console.log(tipoIf(true, 'sí', 'no'));

// Explicación: Devuelve a si cond es true, si no b.