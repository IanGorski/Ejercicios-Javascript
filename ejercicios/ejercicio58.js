// Nivel: 7
// Consigna: Sobrecarga concatenar valores

function concatenar(a, b) {
    return a.toString() + b.toString();
}
console.log(concatenar(1, 'a'));

// Explicación: Convierte ambos valores a string y concatena.