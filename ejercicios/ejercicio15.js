// Nivel: 3
// Consigna: Concatenar dos arrays

function concatenarArrays(a, b) {
    return a.concat(b);
}
console.log(concatenarArrays([1, 2], [3, 4]));

// Explicación: Usa concat para unir dos arrays.